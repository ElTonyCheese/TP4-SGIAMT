@model IEnumerable<SWACTPROY.Models.T_Rol>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.nombreRol)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.detalleRol)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombreRol)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.detalleRol)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idRol">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idRol">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idRol">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
