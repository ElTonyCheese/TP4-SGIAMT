@model IEnumerable<SWACTPROY.Models.T_Rubro>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.nombreRubro)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.detalleRubro)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombreRubro)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.detalleRubro)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idRubro">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idRubro">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idRubro">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
